# The base Playwright image provides chromium + the playwright package.
# We just add FastAPI + httpx for the service shell.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
PyYAML>=6.0
