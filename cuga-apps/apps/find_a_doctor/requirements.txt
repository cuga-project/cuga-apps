# find_a_doctor — standalone Python deps.
# Install: pip install -r requirements.txt
#
# Inline tools only — no MCP servers. Live data via httpx:
#   - Nominatim (OpenStreetMap) geocoding
#   - Overpass (OpenStreetMap) nearby doctors/clinics
#   - DuckDuckGo HTML for review snippets (parsed with regex — no bs4 needed)
# All keyless.

# cuga agent framework (every app builds a CugaAgent)
cuga

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
langchain-core>=0.3
httpx>=0.27

# CUGA policies (intent_guard + tool_guide + output_formatter) use a
# sqlite-vec embedding store. Optional — the app degrades gracefully if
# it's missing (the medical-advice intent_guard then won't be enforced).
sqlite-vec>=0.1.6
