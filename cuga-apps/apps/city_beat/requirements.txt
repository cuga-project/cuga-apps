# City Beat — only what main.py + ui.py import directly. The cuga-apps
# Docker image already provides fastapi/uvicorn/pydantic/langchain through
# requirements.apps.txt, plus langchain-mcp-adapters for _mcp_bridge.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
langchain-core>=0.3
langchain-mcp-adapters>=0.1
