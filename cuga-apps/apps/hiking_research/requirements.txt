# hiking_research — standalone Python deps.
# Install: pip install -r requirements.txt && pip install cuga

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
langchain-core>=0.3

# MCP bridge — calls hosted `geo`, `web` MCPs
mcp>=1.0
langchain-mcp-adapters
