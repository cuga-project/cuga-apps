# recipe_composer — standalone Python deps.
# Install: pip install -r requirements.txt && pip install cuga
#
# Inline tools only — no MCP servers needed.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
langchain-core>=0.3
