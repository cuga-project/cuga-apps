# repo_steward — standalone Python deps.
# Install: pip install -r requirements.txt && pip install cuga
#
# No MCP servers needed. Uses inline tools for sample-repo access and the
# public GitHub REST API via stdlib urllib.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
langchain-core>=0.3
