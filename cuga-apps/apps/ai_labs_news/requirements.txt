# ai_labs_news — standalone Python deps.
# Install: pip install -r requirements.txt
#
# Inline tools only — no MCP servers. Live data via each lab's public RSS/Atom
# feed (httpx fetch + feedparser parse). No API keys.

# cuga agent framework (every app builds a CugaAgent)
cuga

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
langchain-core>=0.3
httpx>=0.27
feedparser>=6.0

# CUGA policies (output_formatter + tool_guide) use a sqlite-vec embedding
# store. Optional — the app degrades gracefully if it's missing.
sqlite-vec>=0.1.6
