# video_qa — standalone Python deps.
# Install: pip install -r requirements.txt && pip install cuga
#
# This app is one of two heavyweight ones (the other is deck_forge). It needs
# chromadb + sentence-transformers + faster-whisper for local video RAG +
# transcription — install will be ~+700 MB on top of the base cuga venv.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
langchain-core>=0.3

# app-specific
chromadb                        # local RAG over video transcripts
sentence-transformers           # embeddings for chromadb
faster-whisper                  # local audio transcription
